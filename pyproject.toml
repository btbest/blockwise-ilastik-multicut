[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "blimp"
version = "0.1.0"
requires-python = ">=3.10"

[project.scripts]
blimp = "blimp:main"

[tool.setuptools]
# Treat each top-level .py file as an importable module (no package directory).
py-modules = [
    "blimp",
    "ilp_reader",
    "fit_classifier",
    "multicut_from_ilp",
]
